<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="0dp">

    <!-- Settings button at the top right -->
    <ImageButton
        android:id="@+id/settings_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/settings"
        android:layout_alignParentTop="true"
        android:layout_alignParentEnd="true"
        android:contentDescription="Settings"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:padding="8dp"/>

    <!-- Profile picture at the top left -->
    <ImageView
        android:id="@+id/profile_picture"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:src="@drawable/ic_profile_pin"
        android:layout_alignParentStart="true"
        android:layout_marginTop="20dp"
        android:layout_marginEnd="16dp"
        android:contentDescription="Profile Picture" />

    <!-- User name below profile picture -->
    <TextView
        android:id="@+id/user_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="User Name"
        android:textSize="20sp"
        android:textStyle="bold"
        android:layout_below="@id/profile_picture"
        android:layout_alignStart="@id/profile_picture"
        android:layout_marginTop="8dp"/>

    <!-- Number of Picchu -->
    <TextView
        android:id="@+id/number_picchu"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Picchu: 0"
        android:layout_below="@id/user_name"
        android:layout_marginTop="12dp"
        android:layout_alignStart="@id/user_name"/>

    <!-- Number of friends -->
    <TextView
        android:id="@+id/number_friends"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Friends: 0"
        android:layout_below="@id/number_picchu"
        android:layout_marginTop="8dp"
        android:layout_alignStart="@id/user_name"/>

    <Button
        android:id="@+id/friend_requests_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Friend Requests"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="16dp"
        android:layout_below="@id/number_friends"/>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/friend_requests_recycler_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:visibility="gone"
        android:layout_below="@id/friend_requests_button"/>

</RelativeLayout>

